export const metadata = {
  title: "Marketing Events Tracker",
  description: "Track marketing events from Excel"
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, fontFamily: "Arial, sans-serif" }}>
        {children}
      </body>
    </html>
  );
}
